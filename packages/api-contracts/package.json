{
  "name": "@cap/api-contracts",
  "version": "1.0.0",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc -w",
    "type-check": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "test:ui": "vitest --ui",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "@cap/shared-types": "workspace:*"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "typescript7": "npm:typescript@^7.0.2",
    "vitest": "^4.0.16"
  }
}
