/**
 * Widget Studio module routes.
 * Phase 1: The Widget Studio is a panel/drawer — no dedicated page route.
 * Routes array is empty for Phase 1; the panel is integrated into the dashboard.
 */
import type { ModuleRouteConfig } from '@cap/shared-types'

export const widgetStudioRouteConfig: ModuleRouteConfig[] = []
