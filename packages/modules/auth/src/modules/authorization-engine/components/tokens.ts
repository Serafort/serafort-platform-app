/**
 * Brand-kit monospace stack for ids, keys, scopes and permission slugs.
 * Kept in one place so every admin screen in the authorization, developer
 * console and access-control modules renders machine identifiers identically.
 */
export const MONO_FONT = '"JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace'
